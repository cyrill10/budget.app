(function(window) {
  window["env"] = window["env"] || {};

  // Environment variables
  window["env"]["auth.username"] = "${AUTH_USERNAME}";
  window["env"]["auth.password"] = "${AUTH_PASSWORD}";
})(this);
