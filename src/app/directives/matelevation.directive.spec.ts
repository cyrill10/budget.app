import { MatelevationDirective } from './matelevation.directive';

describe('MatelevationDirective', () => {
  it('should create an instance', () => {
    const directive = new MatelevationDirective();
    expect(directive).toBeTruthy();
  });
});
